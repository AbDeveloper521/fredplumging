Partner / vendor-system logo files (SVG preferred) go here. Currently rendered as styled wordmarks in TrustBar.tsx and ComplianceSection.tsx.

Partner-platform logos for /about/partners — upload through the Studio
(Partners & Vendor Systems -> Logo image), do not commit them here without a
licence. Files the client should supply, transparent PNG at roughly 400px wide:

  vendorcafe.png
  compliance-depot.png
  vendor-nexus.png
  netvendor.png
  realpage.png

Until uploaded, the cards deliberately render the name as a styled wordmark.
